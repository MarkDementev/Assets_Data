base-url: /data

spring:
  application:
    name: fundAssetsData

  jpa:
    hibernate:
      ddl-auto: create-drop

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master-test.xml